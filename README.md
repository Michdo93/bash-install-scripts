# bash-install-scripts
A few bash installation scripts for Ubuntu, Raspbian and Debian.

## Usage

### Setup

```
git clone https://github.com/Michdo93/bash-install-scripts
cd bash-install-scripts
chmod -R +x *.bash
```

### Install

You can run the installation script with:

```
./install
```

### Uninstall

You can run the deistallation script with:

```
./uninstall
```

## Manually usage

You do not have to download all bash scripts, but can also download the following programs manually as follows:

### AIDE (Advanced Intrusion Detection Environment)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/aide.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/aide.bash -O - | bash
```

### Apache2

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/apache2.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/apache2.bash -O - | bash
```

### BIND (Berkley Internet Naming Daemon)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/bind9.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/bind9.bash -O - | bash
```

### Chkrootkit (Check Rootkit)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/chkrootkit.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/chkrootkit.bash -O - | bash
```

### ClamAV (Clam Anti Virus)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/clamav.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/clamav.bash -O - | bash
```

### Cockpit

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/cockpit.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/cockpit.bash -O - | bash
```

### Dnsmasq

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/dnsmasq.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/dnsmasq.bash -O - | bash
```

### Docker

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/docker.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/docker.bash -O - | bash
```

### Domoticz

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/domoticz.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/domoticz.bash -O - | bash
```

### Dropbear

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/dropbear.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/dropbear.bash -O - | bash
```

### EmulatorJS

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/emulatorjs.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/emulatorjs.bash -O - | bash
```

### Go

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/go.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/go.bash -O - | bash
```

### Grafana

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/grafana.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/grafana.bash -O - | bash
```

### HABApp (Home Automation Bus App) --> Python extension for openHAB (open Home Automation Bus)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/habapp.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/habapp.bash -O - | bash
```

### Home Assistant

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/home_assistant.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/home_assistant.bash -O - | bash
```

### Homer (HOMepage for your servER)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/homer.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/homer.bash -O - | bash
```

### Icinga 2

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/icinga2.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/icinga2.bash -O - | bash
```

### InfluxDB 2

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/influxdb2.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/influxdb2.bash -O - | bash
```

### ISC DHCP server

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/isc-dhcp-server.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/isc-dhcp-server.bash -O - | bash
```

### Jellyfin

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/jellyfin.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/jellyfin.bash -O - | bash
```

### Kea DHCP server

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/kea-dhcp-server.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/kea-dhcp-server.bash -O - | bash
```

### Kodi

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/kodi.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/kodi.bash -O - | bash
```

### Lighttpd

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/lighttpd -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/lighttpd -O - | bash
```

### Linux Dash

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/linux_dash.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/linux_dash.bash -O - | bash
```

### MariaDB

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/mariadb.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/mariadb.bash -O - | bash
```

### Mosquitto

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/mosquitto.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/mosquitto.bash -O - | bash
```

### MySQL

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/mysql.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/mysql.bash -O - | bash
```

### Nextcloud

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/nextcloud.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/nextcloud.bash -O - | bash
```

### NFS kernel server (Network File System kernel server)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/nfs-kernel-server.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/nfs-kernel-server.bash -O - | bash
```

### Nginx (Engine-X)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/nginx.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/nginx.bash -O - | bash
```

### Nmap (Network Mapper)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/nmap.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/nmap.bash -O - | bash
```

### Node.js

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/nodejs.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/nodejs.bash -O - | bash
```

### Open VM Tools

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/open-vm-tools.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/open-vm-tools.bash -O - | bash
```

### openHAB

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/openhab.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/openhab.bash -O - | bash
```

### OpenSSH Server / Client

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/openssh.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/openssh.bash -O - | bash
```

### ownCloud

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/owncloud.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/owncloud.bash -O - | bash
```

### Pi-hole

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/pihole.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/pihole.bash -O - | bash
```

### Plex Media Server

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/plex.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/plex.bash -O - | bash
```

### Portainer

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/portainer.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/portainer.bash -O - | bash
```

### Python

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/python.bash -O - | bash
```

### rkhunter (Rootkit Hunter)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/rkhunter.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/rkhunter.bash -O - | bash
```

### ROS Kinetic (Robot Operating System Kinetic Kame)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/ros-kinetic.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/ros-kinetic.bash -O - | bash
```

### ROS Melodic (Robot Operating System Melodic Morenia)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/ros-melodic.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/ros-melodic.bash -O - | bash
```

### ROS Noetic (Robot Operating System Noetic Ninjemys)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/ros-noetic.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/ros-noetic.bash -O - | bash
```

### Samba Server

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/samba.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/samba.bash -O - | bash
```

### Speedtest CLI (Speedtest Command Line Interface)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/speedtest.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/speedtest.bash -O - | bash
```

### SQLite 3

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/sqlite3.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/sqlite3.bash -O - | bash
```

### ufw (uncomplicated firewall)

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/ufw.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/ufw.bash -O - | bash
```

With opening ports of installed applications:

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/ufw-with-ports.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/ufw-with-ports.bash -O - | bash
```

### VirtualBox

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/virtualbox.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/virtualbox.bash -O - | bash
```

### Visual Studio Code

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/vscode.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/vscode.bash -O - | bash
```

### Webmin

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/webmin.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/webmin.bash -O - | bash
```

### YARA

You can install it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/install/yara.bash -O - | bash
```

You can uninstall it with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/uninstall/yara.bash -O - | bash
```

## do-release-upgrade

You can run a `do-release-upgrade` with:

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/ininstall/do-release-upgrade.bash -O - | bash
```

## Configurations

Because several scripts run automatically and the applications are installed, they are initialized with standard configurations and passwords. These scripts help to customize these configurations.

### Changing Icinga IDO (Icinga Data Output) admin password

The default username and password for Icinga IDO are `icinga_ido` and `icinga_ido`. You can change it with: 

```
./config/icinga2-change-icinga_ido-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/icinga2-change-icinga_ido-pwd.bash -O - | bash
```

### Changing Icinga Web 2 admin password

The default username and password for Icinga Web 2 are `icingaweb2user` and `icingaweb2user`. You can change it with: 

```
./config/icinga2-change-icingaweb2user-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/icinga2-change-icingaweb2user-pwd.bash -O - | bash
```

### Changing InfluxDB 2 admin password

The default username and password for InfluxDB 2 are `admin` and `influxdb`. You can change it with: 

```
./config/influxdb2-change-admin-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/influxdb2-change-admin-pwd.bash -O - | bash
```

### Changing MariaDB root password

The default username and password for MariaDB are `mariadb_root` and `mariadb_root`. You can change it with: 

```
./config/mariadb-change-root-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/mariadb-change-root-pwd.bash -O - | bash
```

### Creating new MariaDB user

```
./config/mariadb-create-new-user.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/mariadb-create-new-user.bash -O - | bash
```

### Changing MySQL root password

The default username and password for MySQL are `mysql_root` and `mysql_root`. You can change it with: 

```
./config/mysql-change-root-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/mysql-change-root-pwd.bash -O - | bash
```

### Creating new MySQL user

```
./config/mysql-create-new-user.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/mysql-create-new-user.bash -O - | bash
```

### Changing Nextcloud admin password

The default username and password for Nextcloud are `nextcloud` and `nextcloud`. You can change it with: 

```
./config/nextcloud-change-nextcloud-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/nextcloud-change-nextcloud-pwd.bash -O - | bash
```
### Changing ownCloud admin password

The default username and password for ownCloud are `owncloud` and `owncloud`. You can change it with: 

```
./config/owncloud-change-owncloud-pwd.bash
```

or

```
wget https://raw.githubusercontent.com/Michdo93/bash-install-scripts/main/config/owncloud-change-owncloud-pwd.bash -O - | bash
```
